@page
@model RegisterConfirmationModel
@{
    ViewData["Title"] = "Potwierdzenie rejestracji | Team-Up";
    ViewData["Description"] = "Team-Up: narzędzie do zakładania/wyszukiwania zespołów umożliwiających realizację start-upów";
    var url = Request.HttpContext.Request.Scheme + "://" + Request.Host;
    var imageUrl = Url.Content(url + "/img/team.png");
    ViewData["Image"] = imageUrl;
}

<div style="width: 100%; margin: 0 auto; text-align: center;">
<h2>Potwierdzenie rejestracji</h2>
@{
    if (@Model.DisplayConfirmAccountLink)
    {
<p>
    <!--
    This app does not currently have a real email sender registered, see <a href="https://aka.ms/aspaccountconf">these docs</a> for how to configure a real email sender.
    Normally this would be emailed: <a id="confirm-link" href="@Model.EmailConfirmationUrl">Click here to confirm your account</a>
    -->
    Sprawdź pocztę e-mail, aby potwierdzić swoje konto. Jeśli nie otrzymałeś wiadomości skontaktuj się z nami poprzez <a href="mailto: kontakt@team-up.pl">kontakt@team-up.pl</a>
</p>
    }
    else
    {
<p>
        Sprawdź skrzynkę pocztową w celu potwierdzenia swojego konta.
</p>
    }
}
</div>
